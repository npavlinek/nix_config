setlocal expandtab
setlocal shiftwidth=4
setlocal softtabstop=4
setlocal tabstop=4

setlocal spell

" Used by `gf` for finding files. It adds a `.md` file extension if a file isn't found.
setlocal suffixesadd=.md
